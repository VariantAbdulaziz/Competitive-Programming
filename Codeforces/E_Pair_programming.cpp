#include <bits/stdc++.h>
#define int int64_t
#define all(a) a.begin(), a.end()
#define range(a, f, t) a.begin() + f, a.begin() + t

using namespace std;

void solve() {
    int n, k, row;
    cin >> n >> k;
    row = 2 * n;
    vector<int> a(row);
    for (auto &aj: a) cin >> aj;
}

int32_t main(){
    solve();
}

