#include <bits/stdc++.h>
#define int int64_t
#define all(a) a.begin(), a.end()
#define range(a, f, t) a.begin() + f, a.begin() + t

using namespace std;

void solve(){
    int n, q;
    vector<int> p(n + 1), a(n + 1);
    for(int i=1; i<=n; i++) {
        cin >> p[i];
    }
    for(int i=2; i<=n; i++) {
        cin >> a[i];
    }
    
}

int32_t main(){
    solve();
}
