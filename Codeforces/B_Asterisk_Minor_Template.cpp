#include <bits/stdc++.h>
#define int int64_t
#define all(a) a.begin(), a.end()
#define range(a, f, t) a.begin() + f, a.begin() + t

using namespace std;

void solve(){
    string a, b;
    cin >> a >> b;
}

int32_t main(){
    int t;
    cin >> t;
    while(t--) {
        solve();
        cout << endl;
    }
}
