#include <bits/stdc++.h>
#define int int64_t
#define all(a) a.begin(), a.end()
#define range(a, f, t) a.begin() + f, a.begin() + t

using namespace std;

void solve()
{
    int n;
    cin >> n;
    vector<int> a(n), b(n);
    for (auto &aj : a)
        cin >> aj;
    for (auto &bj : b)
        cin >> bj;
}

int32_t main()
{
    int t;
    cin >> t;
    while (t--)
        solve();
}
